name: testing
on:
  push:
jobs:
  myfirstjob:
    runs-on: ubuntu-latest
    steps:
      - name: checking out the code
        uses: actions/checkout@v4

      - name: run the command
        run: |
          ls -latr
          cat README.md

